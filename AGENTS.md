# Projekt1 team instructions

Read docs/COMMAND-CENTER.md, docs/ARCHITECTURE.md, contracts/index.ts, and your named brief in docs/team before changing application code.

- Ben owns shared contracts, backend/auth/persistence, integration, dependencies, and deployment. Nicolas owns frontend/rendering. Shaw owns retrieval/adapters. Shreev owns graph/AI.
- Preserve teammates' existing work. Inspect the current branch and working tree before edits; never reset, overwrite, or scaffold over another person's checkout.
- Real product/demo data only. No fictional seed, filler nodes or fabricated paths. Anonymous algorithm unit tests must stay tests and never populate the app. Never commit personal imports, credentials or tokens to this public repository.
- Separate observed contacts/follows, identity proposals, and supported introduction relationships. An LLM does not establish truth by assertion. No silent fuzzy identity merge.
- Authorize scope before graph search and evidence access. Do not pool users' networks just because they signed in. Respect source-specific restrictions and explicit sharing choices.
- contracts/index.ts is the shared wire boundary. Types are not runtime validation; Ben must implement validators. Coordinate changes before editing shared contracts or lockfiles.
- Frontend only renders graph/search events; Shreev's engine ranks paths. Stream/replay events must represent real computation or committed imports.
- Work only on the track authorized by your human. Subagents may perform bounded tasks in your ownership area; avoid parallel edits in the same checkout. No cross-track scope expansion without coordinator agreement.
- Report branch/commit, files changed, verified behavior, test results, contract changes requested, blockers and next handoff after each milestone.
- No automatic outreach messages, unrestricted network crawling, account-recovery probing or credentials/session-cookie collection.
- Deadline: Sunday September 6, 2026, 2 p.m. America/Los_Angeles. Feature freeze noon; full rehearsal 1 p.m.
