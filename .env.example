# Turso is for local/portable development only. Never commit the real token.
TURSO_DATABASE_URL=libsql://connection-graph-shreev2703.aws-us-west-2.turso.io
TURSO_AUTH_TOKEN=replace-with-a-token-from-turso
# Optional: set a stable ID to opt authenticated users into one shared demo graph.
# Leave unset for private per-user libraries.
# ORBIT_SHARED_WORKSPACE_ID=demo-knowledge-graph
# Per authenticated contributor. Defaults: 20 writes/minute, 120 reads/minute.
# ORBIT_WRITE_LIMIT_PER_MINUTE=20
# ORBIT_READ_LIMIT_PER_MINUTE=120
